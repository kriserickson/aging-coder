<!DOCTYPE html>
<html lang="en">
<head>
{% include "head.njk" %}
</head>
<body class="home-template">
<header class="site-header">
  <div class="site-header-inner site-container">
    <nav class="site-nav" aria-label="Main navigation">
        <div class="site-logo"><a href="/" aria-label="Home"><img src="/img/{{ site.logo }}" alt="Blog Logo" class="site-logo-img" loading="lazy" decoding="async"></a></div>
        <ul>
            {% for link in site.links %}
                <li><a href="{{ link.url }}">{{ link.title }}</a></li>
            {% endfor %}
            <li>
                <button id="theme-toggle" aria-label="Toggle dark mode"><span id="theme-toggle-icon" aria-hidden="true"></span></button>
            </li>
        </ul>
    </nav>
    <div>
        <h1 class="site-title">{{ site.title }}</h1>
        <h2 class="site-description">{{ site.description }}</h2>
    </div>
  </div>
</header>

<div class="site-container">
<main>
    {{ content | safe }}
</main>

{% include "footer.njk" %}
</div>
{% include "scripts.njk" %}
</body>
</html>
