<!DOCTYPE html>
<html lang="en">
<head>
    {% include "head.njk" %}
</head>
<body class="home-template">
<header class="site-header">
    <div class="site-header-inner">
        <nav class="site-nav" aria-label="Main navigation">
            <div class="site-logo"><a href="/" aria-label="Home"><img src="/img/{{ site.logo }}" alt="Blog Logo"
                                                                      class="site-logo-img" loading="lazy"
                                                                      decoding="async"></a></div>
            <ul>
                {% for link in site.links %}
                    <li id="{{link.id}}"><a href="{{ link.url }}">{{ link.title }}</a></li>
                {% endfor %}
                <li>
                    <button id="theme-toggle" aria-label="Toggle dark mode">
                        <span id="theme-toggle-icon">
                            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" focusable="false" aria-hidden="true">
                                <path
                                        d="M12 3v2M12 19v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"
                                    stroke="currentColor" stroke-width="1.5" stroke-linecap="round"
                                    stroke-linejoin="round"/>
                            </svg>
                        </span>
                    </button>
                </li>
            </ul>
        </nav>
        <div>
            <h1 class="site-title">{{ site.title }}</h1>
            <h2 class="site-description">{{ site.description }}</h2>
        </div>
    </div>
</header>

<div class="site-container">
    <main>
        {{ content | safe }}
    </main>
</div>

{% include "footer.njk" %}

{% include "scripts.njk" %}
</body>
</html>
